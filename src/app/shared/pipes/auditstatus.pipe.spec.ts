import { AuditstatusPipe } from './auditstatus.pipe';

describe('AuditstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
